apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: k8s-debug
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/ranenachiket/k8s-app
    targetRevision: HEAD
    path: devops/deployments/debug
  destination:
    server: https://kubernetes.default.svc
    namespace: debug

  syncPolicy:
    syncOptions:
    - CreateNamespace=true

    automated:
      selfHeal: true
      prune: true